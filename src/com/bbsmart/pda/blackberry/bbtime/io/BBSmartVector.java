package com.bbsmart.pda.blackberry.bbtime.io;

import java.util.Vector;

import net.rim.device.api.util.Persistable;

public class BBSmartVector extends Vector implements Persistable {
	// Vector duplicate sitting inside BBSmart package namespace
	// to ensure persistence store is wiped on uninstall
}
